# pitstop
Pitstop scans the current repo for obvious and uncaught bugs. It uses Modelhitch to rotate coding models through the codebase and collect their findings. The results are saved as a structured handoff for the next agent.
